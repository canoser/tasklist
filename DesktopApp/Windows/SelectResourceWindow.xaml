<Window x:Class="planlama_app.Windows.SelectResourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Kaynak Bağla" Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Border Background="{DynamicResource MaterialDesignPaper}"
            CornerRadius="8"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="4"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Başlık -->
            <Border Grid.Row="0" Background="{DynamicResource MaterialDesignPrimaryDarkBrush}" CornerRadius="8,8,0,0" Padding="16,12">
                <TextBlock x:Name="TxtHeaderTitle" Text="Mevcut veya Yeni Kaynak Bağla" Foreground="White" FontSize="15" FontWeight="SemiBold"/>
            </Border>

            <!-- İçerik -->
            <StackPanel Grid.Row="1" Margin="20,16,20,0">
                <TextBlock Text="Mevcut kaynaklardan birini seçebilir veya aşağıdan hızlıca yeni kaynak oluşturabilirsiniz:" 
                           TextWrapping="Wrap" Margin="0,0,0,14" FontSize="12.5" Opacity="0.85"/>

                <!-- Mevcut Kaynak Seçimi -->
                <ComboBox x:Name="CmbResources"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          DisplayMemberPath="Title"
                          SelectedValuePath="Id"
                          Foreground="{DynamicResource MaterialDesignBody}"
                          materialDesign:HintAssist.Hint="Mevcut Kaynak Seçin"
                          Margin="0,0,0,16">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>

                <Separator Style="{StaticResource MaterialDesignLightSeparator}" Margin="0,0,0,16"/>

                <!-- VEYA Yeni Kaynak Ekleme Alanı -->
                <Expander x:Name="ExpNewResource" Header="➕ Veya Yeni Kaynak Oluştur" Margin="0,0,0,8" IsExpanded="False">
                    <StackPanel Margin="8,8,8,4">
                        <TextBox x:Name="TxtNewTitle" 
                                 materialDesign:HintAssist.Hint="Kaynak Başlığı (Örn: Doküman Linki)"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,10"/>
                        <TextBox x:Name="TxtNewUrl" 
                                 materialDesign:HintAssist.Hint="URL / Açıklama Metni (Örn: http://...)"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,0,6"/>
                    </StackPanel>
                </Expander>
            </StackPanel>

            <!-- Butonlar -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16">
                <Button Content="Temizle / Bağlantıyı Kaldır" Click="BtnClear_Click" Style="{StaticResource MaterialDesignFlatButton}" Foreground="OrangeRed" Margin="0,0,8,0"/>
                <Button Content="İptal" Click="BtnCancel_Click" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
                <Button Content="Tamam" Click="BtnOk_Click" Style="{StaticResource MaterialDesignFlatMidBgButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
