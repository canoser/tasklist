<Window x:Class="planlama_app.Windows.SelectCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Kategori Seç" Width="350" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize">
    
    <Border Background="{DynamicResource MaterialDesignPaper}"
            CornerRadius="8"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Başlık Çubuğu -->
            <Border Grid.Row="0" Background="{DynamicResource MaterialDesignPrimaryDarkBrush}" CornerRadius="8,8,0,0" Padding="16,12">
                <TextBlock Text="Hedef Kategoriyi Seçin" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
            </Border>

            <!-- İçerik -->
            <StackPanel Grid.Row="1" Margin="24,16,24,0">
                <TextBlock Text="İçe aktarılacak görevler hangi kategoriye eklensin?" TextWrapping="Wrap" Margin="0,0,0,12" FontSize="13" Foreground="{DynamicResource MaterialDesignBody}"/>
                <ComboBox x:Name="CmbCategories" 
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          DisplayMemberPath="Name"
                          SelectedValuePath="Id"
                          materialDesign:HintAssist.Hint="Kategori"/>
            </StackPanel>

            <!-- Butonlar -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16">
                <Button Content="İptal" Click="BtnCancel_Click" Margin="0,0,8,0" Style="{StaticResource MaterialDesignFlatButton}"/>
                <Button Content="Seç ve Devam Et" Click="BtnOk_Click" Style="{StaticResource MaterialDesignFlatButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
