<Window x:Class="planlama_app.Windows.MessageDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Mesaj" Width="450" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize">
    
    <Border Background="{DynamicResource MaterialDesignPaper}"
            CornerRadius="8"
            BorderBrush="{DynamicResource MaterialDesignDivider}"
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Başlık Çubuğu -->
            <Border Grid.Row="0" Background="{DynamicResource MaterialDesignPrimaryDarkBrush}" CornerRadius="8,8,0,0" Padding="16,12">
                <TextBlock x:Name="TxtTitle" Text="Başlık" Foreground="White" FontSize="16" FontWeight="SemiBold"/>
            </Border>

            <!-- İçerik -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="24">
                <materialDesign:PackIcon x:Name="IconMessage" Kind="Information" Width="48" Height="48" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="{DynamicResource MaterialDesignPrimaryBrush}"/>
                <TextBlock x:Name="TxtMessage" Text="Mesaj içeriği burada yer alacak." TextWrapping="Wrap" VerticalAlignment="Center" FontSize="14" Foreground="{DynamicResource MaterialDesignBody}"/>
            </StackPanel>

            <!-- Butonlar -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16" x:Name="BtnContainer">
                <Button x:Name="BtnYes" Content="Evet" Click="BtnYes_Click" Margin="0,0,8,0" Visibility="Collapsed" Style="{StaticResource MaterialDesignFlatButton}"/>
                <Button x:Name="BtnNo" Content="Hayır" Click="BtnNo_Click" Margin="0,0,8,0" Visibility="Collapsed" Style="{StaticResource MaterialDesignFlatButton}"/>
                <Button x:Name="BtnCancel" Content="İptal" Click="BtnCancel_Click" Margin="0,0,8,0" Visibility="Collapsed" Style="{StaticResource MaterialDesignFlatButton}"/>
                <Button x:Name="BtnOk" Content="Tamam" Click="BtnOk_Click" Visibility="Collapsed" Style="{StaticResource MaterialDesignFlatButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
