<Window x:Class="planlama_app.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Giriş" Height="200" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Style="{StaticResource MaterialDesignWindow}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtPrompt" Grid.Row="0" Text="Değer giriniz:" Margin="0,0,0,16"/>
        
        <TextBox x:Name="TxtInput" Grid.Row="1" 
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 VerticalAlignment="Top" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="İptal" Style="{StaticResource MaterialDesignFlatButton}" Click="BtnCancel_Click" Margin="0,0,8,0"/>
            <Button Content="Tamam" Style="{StaticResource MaterialDesignFlatMidBgButton}" Click="BtnOk_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
